pipeline {
    agent any 
    stages { 
        stage('Checkout Code') { 
            steps { 
                git branch: 'main',
                    url: 'https://github.com/Prathi0611/calculator.git'
            } 
        } 
        stage('Show Files') { 
            steps { 
                bat 'dir' 
            } 
        } 
    } 
}
